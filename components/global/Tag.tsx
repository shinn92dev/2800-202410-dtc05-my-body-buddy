type TagProps = {
    text: string;
    selected: boolean;
};

export default function Tag({ text, selected }: TagProps) {
    return (
        {/* returns white-text-gray-background tag if selected, opposite otherwise */}
);
}